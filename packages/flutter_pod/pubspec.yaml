name: flutter_pod
description: Typedefs for riverpod
version: 0.14.0
homepage: https://github.com/TimWhiting/pod

environment:
  sdk: ">=2.13.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^0.14.0
  pod: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# dependency_overrides:
#   pod:
#     path: ../pod

flutter:
